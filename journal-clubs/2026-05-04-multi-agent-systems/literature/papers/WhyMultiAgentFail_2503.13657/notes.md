# Notes

This paper matters because it gives coding agents a shared failure vocabulary for debugging multi-agent systems.

Use it when diagnosing orchestration failures, designing verifier roles, comparing MAS frameworks, or explaining why coordination can fail even when individual LLM calls look competent.

Key terms: MAST, MAST-Data, system design issues, inter-agent misalignment, task verification, step repetition, premature termination, reasoning-action mismatch, LLM-as-a-judge.
