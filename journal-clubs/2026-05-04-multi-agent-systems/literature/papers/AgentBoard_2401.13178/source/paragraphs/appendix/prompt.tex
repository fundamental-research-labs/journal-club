\section{Prompt Details \label{appendix:prompt}}
% \jh{These prompt figures are actually not unified, for example, sometimes the "ACTION" "OBSERVATION" are all capitalized and sometimes not, sometimes empty lines exist between different steps. plz fix it if these are latex copy-paste errors. }
%\jh{this section is too long, plz only list 9 prompts for the 9 tasks respectively, and omit some intermediate steps if the one-shot example is too long (when doing so, plz indicate like ``[xx intermediate steps are omitted]'')}
As shown in Figure~\ref{fig:prompt_template},
we use a unified prompt template for different tasks in \BenchName{}.
Basically, a prompt consists of 5 parts.
\textcolor{blue}{\{System Prompt\}} represents the system prompt for the LLM, such as ``You are a helpful AI agent''.
\textcolor{blue}{\{Instruction\}} mainly consists of task descriptions and action definitions.
\textcolor{blue}{\{Examples\}} represents in-context learning examples.
\textcolor{blue}{\{Goal\}} is the current goal that needs to be accomplished, and \textcolor{blue}{\{Trajectory\}} is the interaction history between the LLM agent and the environment.

For different tasks, the contents of these five parts are different.
Prompt details for Embodied AI tasks are shown in Figure~\ref{fig:alfworld_prompt}, \ref{fig:scienceworld_prompt} and \ref{fig:babyai_prompt}.
Prompt details for Game tasks are shown in Figure~\ref{fig:jericho_prompt} and \ref{fig:pddl_prompt}.
Prompt details for Web tasks are shown in Figure~\ref{fig:webshop_prompt} and \ref{fig:webarena_prompt}.
Prompt details for Tool tasks are shown in Figure~\ref{fig:academia_prompt} and \ref{fig:todo_prompt}, respectively.
%\ref{fig:weather_prompt},\ref{fig:movie_prompt},and \ref{fig:sheet_prompt}
\input{prompt/template}
\input{prompt/alfworld}
\input{prompt/scienceworld}
\input{prompt/babyAI}
\input{prompt/Jericho}
\input{prompt/pddl}
\input{prompt/webshop}
\input{prompt/webarena}
% \input{prompt/weather}
% \input{prompt/movie}
\input{prompt/academia}
\input{prompt/todo}
% \input{prompt/sheet}