% \section{Evaluation Metric\label{sec: Evaluation Metric}}

% For WebShop, we extend the original algorithm to continuously score by identifying different pages.
% For WebArena, the matching score is based on URL similarity and content completion.
% For Sheet, we evaluate the matching score between two sheets at the cell level by comparing their contents against a reference sheet.
% Specifically, the match score is computed by dividing the number of matching cells by the total number of cells examined.
% This ratio provides a fine-grained score representing the percentage of agreement between the sheets.
% For other tasks in \BenchName, we employ the ratio of achieved of sub-goals.
