\section{Main Results}
\label{sec:main-results}
\subsection{Evaluation Benchmarks}
We evaluate \ourmethod on two long-horizon software engineering benchmarks that require agents to coordinate multiple interdependent edits over shared repositories.

\subsection{Commit0}
\label{sec:commit0}
Commit0 \cite{zhao2024commit0} tests whether agents can implement a Python library from scratch given a repository skeleton and a suite of unit tests. The task is considered successful only if all tests pass, making it a repository-level integration problem rather than a collection of independent code completions. We use Commit0-Lite as our primary evaluation set, following the official leaderboard setup.\footnote{\url{https://commit-0.github.io/}} 

In Commit0, the manager receives an instruction and the path to a repository directory that contains executable tests. We provide the user instruction in Appendix \ref{app:commit0-prompt}. The manager first checks the import statements to identify the file-level dependencies, collects executable test cases from the repository, and examines which files those tests exercise. These tests indicate which files are required for specific tests to pass and help the manager understand the expected behavior of the overall implementation task. Based on these explorations, the manager can identify which components need to be implemented earlier so that dependent tests can pass. When delegating tasks, the manager first considers delegating at the file level.. However, if a single file contains a large number of unimplemented functions, the manager can further divide the work at the function level, ensuring that the function sets assigned to different engineers do not overlap.

After assigning the first tasks to multiple engineers, the manager can continue to explore the repository and optimizing the rest of task delegation plan until one engineer completes the current tasks, submits a commit for merge, and is ready for the next task.

% In addition, we construct \emph{Commit0-Heavy}, a subset consisting of the eight repositories with the largest number of unit tests. We consider these to be more complex instances because these repositories typically involve a greater number of source files, cross-file dependencies, and interrelated functionalities. As a result, they impose stronger long-horizon requirements and more pronounced multi-agent coordination challenges, particularly in terms of dependency management and integration reliability.
\subsection{PaperBench}
\label{sec:paperbench}
PaperBench \cite{starace2025paperbench} evaluates an agent's ability to reproduce the main contributions of a published conference paper, typically involving multi-step implementation, experimental setup, and result verification. The benchmark emphasizes long-horizon reasoning and structured execution over complex codebases. Due to computational cost constraints, we adopt the \texttt{Code-Dev} evaluation protocol instead of running the full evaluation pipeline. Following the benchmark’s evaluation paradigm, we use \texttt{gpt-5-mini} \citep{openai2025gpt5mini} as the judge model to assess functional correctness and completion quality.

As an open-ended task, explicit test-to-file mappings are not always available. The manager reads the paper by considering the main contribution described in the paper as the central implementation objective and infers the required implementation order from it. We provide the prompt in Appendix \ref{app:paperbench-prompt}.


% The inputs differ across benchmarks. For Commit0, the manager receives an instruction and the path to a repository directory that contains executable tests. For PaperBench, the manager receives a research paper and reproduction requirements, which typically do not provide explicit test-to-file mappings.
% We define the basic implementation unit at the file or module level. Before delegating work to engineers, the manager analyzes how these units depend on each other. 
% For Commit0 tasks, it inspects import statements to identify file-level dependencies. It also collects executable test cases from the repository and examines which files they exercise. These tests indicate which files are required for specific tests to pass and help the manager understand the expected behavior of the task. This allows the manager to identify which components must be implemented earlier so that dependent tests can pass. For open-ended tasks such as PaperBench, explicit test-to-file mappings are not always available. The manager reads the paper by considering the main contribution described in the paper as the central implementation objective and infers the required implementation order from it. Although the granularity differs, in both settings, the manager constructs a dependency structure before delegation. Engineers are assigned tasks only after this dependency structure is established. \jy{maybe we don't want it to be too task specific?}

% \subsection{Experimental Setup}
% We build \ourmethod using the open-source OpenHands agent SDK~\citep{wang2024openhands,wang2025openhands} (v1.11.0). \ourmethod instantiates a centralized manager responsible for dependency-aware task delegation and multiple software-engineer agents operating in isolated workspaces. We evaluate \ourmethod with three language models: two open-source models (\texttt{GLM 4.7} \citep{zeng2025glm} and \texttt{MiniMax 2.5} \citep{minimax2024minimax25}) and one closed-source model (\texttt{Claude-4.5-Sonnet} \citep{anthropic2024claude45}). For a fair comparison, we follow the Commit0 leaderboard configuration and compare against a single-agent baseline with $\texttt{max\_iterations}=100$ on both Commit0 and PaperBench. We compare against a single-agent baseline to measure what branch-and-merge adds on top of the same underlying agent setup, without introducing additional differences from other multi-agent frameworks. For multi-agent runs, we set $\texttt{max\_iterations}=50$ for the central manager and $\texttt{max\_iterations}=80$ for each software-engineer agent. For both Commit0 and PaperBench, we set the number of implementation rounds to $2$. In the main results we use one central manager with $2$ engineer agents on PaperBench and $4$ engineer agents on Commit0. We provide more detailed analysis of the configurations in Section \ref{sec:analysis}.\footnote{All configurations are fixed prior to experimentation to balance final performance (correctness) and runtime efficiency.}

% \subsection{Baselines}


\subsection{Experimental Setup}
We build \ourmethod using the open-source OpenHands agent SDK~\citep{wang2024openhands,wang2025openhands} (v1.11.0). \ourmethod instantiates a centralized manager responsible for dependency-aware task delegation and multiple software-engineer agents operating in isolated workspaces. We evaluate \ourmethod with three language models: two open-source models (\texttt{GLM 4.7}~\citep{zeng2025glm} and \texttt{MiniMax 2.5}~\citep{minimax2024minimax25}) and one closed-source model (\texttt{Claude-4.5-Sonnet}~\citep{anthropic2024claude45}).

Following the Commit0 leaderboard configuration, we use a single-agent setup with $\texttt{max\_iterations}=100$ on both Commit0 and PaperBench. For multi-agent runs, we set $\texttt{max\_iterations}=50$ for the central manager and $\texttt{max\_iterations}=80$ for each software-engineer agent. For both Commit0 and PaperBench, we use $2$ implementation rounds. In the main results, we use one central manager with $2$ engineer agents on PaperBench and $4$ engineer agents on Commit0. We provide a more detailed analysis of configuration choices in Section~\ref{sec:analysis}.\footnote{All configurations are fixed prior to experimentation to balance correctness and runtime efficiency.}

\subsection{Baselines}
Our primary baseline is a matched single-agent system built on the same OpenHands agent. We use this baseline to isolate the effect of branch-and-merge coordination while holding the underlying agent framework fixed. This controlled comparison allows us to measure the incremental contribution of dependency-aware delegation, isolated workspaces, and merge-and-branch integration without introducing additional variation from framework-level differences such as prompting structure, tool interfaces, memory mechanisms, or execution policies.

We therefore do not treat the main evaluation as a benchmark across heterogeneous multi-agent frameworks. Instead, our goal is to test whether branch-and-merge coordination improves software-engineering performance within a fixed agent substrate. To further analyze this design choice, we include ablations in Section~\ref{sec:analysis} that vary coordination and isolation mechanisms within the same stack.

% \jy{add explanation of why different N engineers}

% \gncomment{I think that we'll want to explain the main results table first.}

% \subsection{Structural Limits of Iterative Single-Agent}



\subsection{Branch-and-Merge Based Coordination Improves Multi-Agent Performance}
\input{tables/main_results}

% We compare single-agent baselines with \textsc{Caid} in Table \ref{tab:main-results} and observe a consistent advantage for structured multi-agent execution on both benchmarks. On PaperBench, we observe the clearest signal is that multi-agent coordination can unlock large gains for weaker single-agent runs, for example MiniMax 2.5 reaches 37.1\% under multi-agent while its single-agent score is only 10.4\%. At the same time, the benefit is not restricted to weak baselines: even with Claude 4.5, our multi-agent coordination yields a higher score than the single-agent baseline (63.3\% versus 57.2\%). On Commit0-Lite, the same direction holds in Table \ref{tab:main-results}. Claude 4.5 improves to 59.1\% under multi-agent compared with 53.1\% for single-agent, and MiniMax 2.5 reaches 57.0\% under multi-agent. These comparisons show that the performance gap is not explained by changing the underlying model, but by changing the multi-agent coordination structure. \textsc{Caid} with a central manager to control the overall implementation progress which makes asynchronous and parallel work usable by isolating engineers in independent worktrees and enforcing integration through explicit merge and test validation, so progress consolidates into a coherent, test-passing repository state instead of being destabilized by long-horizon error accumulation.

We compare \ourmethod with the single-agent baseline in Table \ref{tab:main-results} and observe a consistent advantage for the branch-and-merge-based multi-agent system across both benchmarks and three LLMs. On PaperBench, we observe that multi-agent coordination yields large gains for weaker single-agent runs: MiniMax 2.5 reaches 36.7\% under multi-agent execution, while its single-agent score is only 10.4\%. The improvement is not limited to weaker models. With Claude 4.5, multi-agent execution achieves 63.3\% compared to 57.2\% for single-agent. In Commit0-Lite, we find the same pattern. Claude 4.5 improves from 53.1\% to 59.1\%, and MiniMax 2.5 reaches 57.0\% under multi-agent execution. These results indicate that the performance gap is not explained by changing the underlying model, but by changing the execution method. In \ourmethod, engineers work in separate branches and changes enter the main branch only through explicit merge and test validation. This makes parallel work usable by separating implementation from integration: engineers can iterate locally without overwriting each other’s intermediate states, while integration failures are surfaced at merge time with concrete test signals tied to specific updates.  Our results in Table \ref{tab:main-results} are consistent with the benefit of making integration explicit and test-gated under long-horizon execution. We provide one-sided t-tests in Appendix \ref{app:stats-test}.



Table \ref{tab:main-results} further reveals an important strategic implication. In long-horizon shared-artifact tasks, multi-agent coordination should not be treated as a fallback after single-agent failure. The Single-Agent + Multi-Agent setting approximates a practical strategy in which a single agent is first attempted, followed by coordinated execution if necessary. However, this sequential strategy incurs nearly additive runtime and cost, while the final performance remains close to the direct multi-agent result. For example, on PaperBench with Claude Sonnet 4.5, the combined strategy reaches 66.8\%, only slightly above the multi-agent score of 63.3\%, yet runtime increases from 2080.4s to 3883.9s and cost rises from 9.3 to 12.6. On Commit0-Lite with MiniMax 2.5, the multi-agent score is 57.0\%, and the combined strategy remains 57.0\%, while both runtime and cost increase substantially. These results give us a clear strategy insight for long-horizon shared-artifact tasks. 
Treating multi-agent coordination as a fallback after a single-agent attempt is inefficient. A more cost-effective strategy is to adopt coordinated multi-agent execution from the outset rather than switching only after failure.

% \gncomment{The rest of this paragraph felt pretty repetitive. It could maybe just be deleted, or at least condensed significantly.} Treating multi-agent coordination as a fallback after a single-agent attempt is inefficient. The Single-Agent + Multi-Agent strategy largely adds the runtime and cost of the initial single-agent phase, while yielding a final score that is close to directly running multi-agent execution. In other words, sequential escalation typically pays for an extra single-agent run without a proportional performance return. For interdependent, test-gated tasks, a more cost-effective strategy is to adopt coordinated multi-agent execution from the outset rather than switching only after failure.





% \subsection{Strategy Implications for Long-Horizon SWE Tasks}

% \gncomment{This seems pretty minor for a section, could we just incorporate it into the previous section?}

% The third column in Table \ref{tab:main-results} reveals a clear strategic implication. In long-horizon shared-artifact tasks, multi-agent coordination should not be treated as a fallback after single-agent failure. The Single-Agent + Multi-Agent setting approximates a common practical strategy: first attempt the task with a single agent, and switch to coordinated execution only if the initial attempt does not succeed. Table \ref{tab:main-results} shows that this strategy incurs nearly additive runtime and cost, while the final performance remains close to the direct multi-agent result. For example, on PaperBench with Claude Sonnet 4.5, the single-agent score is 57.2\%, and the multi-agent score is 63.3\%. The combined strategy reaches 66.8\%, only slightly above the multi-agent result, but the runtime increases from 2080.4s to 3883.9s, and the cost rises from 9.3 to 12.6. For GLM 4.7 on PaperBench, the multi-agent score is 45.4\%, and the combined strategy reaches 48.5\%, while runtime increases from 1449.4s to 2626.9s and cost from 4.7 to 7.5.  On Commit0-Lite, the pattern is even clearer. For MiniMax 2.5, the multi-agent score is 57.0\%, and the combined strategy remains 57.0\%, yet runtime increases from 1908.6s to 2660.7s and cost from 4.5 to 6.1. In this case, the additional single-agent phase does not contribute to a performance gain while strictly increasing the computation. These results reveal that the combined strategy does not compound performance, but largely accumulates computation. The final score is effectively determined by the stronger execution, whereas runtime and cost reflect the sum of both phases. In practice, this means paying the full cost of a single-agent attempt without obtaining a proportional performance improvement. For long-horizon tasks characterized by interdependent components and test-gated integration, we therefore advocate directly adopting SWE-primitive-driven multi-agent coordination rather than sequentially escalating from single-agent execution. Early isolation and structured integration are not optional optimizations, but necessary conditions for efficient progress.


\subsection{Single Agents Fail to Utilize More Iterations}
% \gncomment{If we discuss the main content before discussing this section, an alternative way to present the results would be a bar chart showing the delta over }
\input{figs/single-agent-iter}
\textbf{\textit{Can a single agent overcome long-horizon shared-artifact challenges simply by running longer?}} 
To study this, we run a single agent with $max\_iterations=100$ and $max\_iterations=200$. We control computation through a max iteration budget rather than enforcing a fixed runtime, which better reflects practical agent deployment where iteration-based control is commonly used. As shown in Figure \ref{fig:single-agent-iter}, doubling the iteration limit yields only marginal improvements in the final performance and, in some cases, even degraded results. In PaperBench, $\Delta$ from 100 to 200 iterations remains small for GLM 4.7 and MiniMax 2.5, and becomes negative for Claude Sonnet 4.5. In Commit0-Lite, the improvement is similarly limited, and MiniMax 2.5 shows a negative delta when the iteration budget is increased. This trend is consistent with the findings reported in PaperBench, where forcing the agent to run until a time limit does not reliably improve the judge score \citep{starace2025paperbench}. In Figure \ref{fig:single-agent-iter}, we also show the score gain obtained by \ourmethod relative to the baseline of 100 iterations of a single-agent. Across both benchmarks, these gains are substantially larger than those achieved simply by increasing the single-agent iteration budget. For example, on PaperBench the multi-agent improvement for MiniMax 2.5 exceeds 25 percentage points, while doubling the iteration limit yields only a small change. A similar gap appears in Commit0-Lite. These results show that extending the iteration budget alone does not resolve the fundamental bottleneck of a single agent and does not reliably improve final performance on long-horizon tasks, whereas multi-agent coordination produces significantly larger gains under the same baseline reference.

% \textbf{\textit{Can a single agent overcome long-horizon shared-artifact challenges simply by running longer?}} To study this, we run a single agent with $max\_iterations=100$ and $max\_iterations=200$. We control computation through a max iteration budget rather than enforcing a fixed runtime, which better reflects practical agent deployment where iteration-based control is commonly used. As shown in Figure \ref{fig:single-agent-iter-paperbench}, doubling the iteration limit yields only marginal improvements in final performance, and in some cases even degraded results. This trend is consistent with the findings reported in PaperBench, where forcing the agent to run until a time limit does not always improve the judge score \citep{starace2025paperbench}. From Figure \ref{fig:single-agent-iter-paperbench} and Figure \ref{fig:single-agent-iter-commit0}, we further observe that single agents usually do not fully utilize the increased iteration budgets. When the budget is doubled, the increase in actual iterations is small relative to the additional allowance, which indicates diminishing returns from simply extending the run. This pattern suggests that the bottleneck is not an insufficient iteration allowance but instability in the execution trajectory itself. In practice, we observe that single agents frequently terminate early despite the remaining iteration budget and exhibit a form of lazy behavior. We provide examples in the appendix \ref{}. In contrast, in the multi-agent setting, Figure \ref{fig:single-agent-iter-paperbench} and Figure \ref{fig:single-agent-iter-commit0} show a clear and consistent trend. Multi-agent systems utilize more iterations and result into more tests passed at the end compared with the single agent. These results imply that for long-horizon complex tasks, simply increasing iteration limits is insufficient and that a well-designed multi-agent system is necessary.



% Can a single agent overcome long-horizon shared-artifact challenges simply by running longer? To answer this, we increase the maximum iteration budget from 100 to 200 while keeping the model and task fixed. Across both Commit0 and PaperBench, doubling the iteration limit yields only marginal improvements in final score. On Commit0, GLM 4.7 increases from 58.5 to 60.2, MiniMax 2.5 from 46.5 to 47.6, and Claude 4.5 from 49.6 to 51.2. On PaperBench, GLM 4.7 improves from 38.0 to 41.2 and Claude 4.5 from 57.2 to 59.2, while MiniMax 2.5 rises from 10.4 to 17.6 but remains substantially below competitive performance. These gains are small relative to the doubled iteration budget, indicating clear diminishing returns. Moreover, the actual number of executed iterations does not increase proportionally when the cap is raised; in many cases it remains around 88 to 92 steps even when 200 iterations are permitted. This pattern indicates that the bottleneck is not an insufficient iteration allowance, but instability in the execution trajectory itself. In practice, we observe that single agents frequently terminate early despite remaining iteration budget, exhibiting a form of lazy behavior; representative examples are provided in the appendix. As task scope and dependency depth grow, the agent must interleave planning, implementation, debugging, and verification within a single evolving context, causing earlier design decisions to drift and local fixes to introduce new inconsistencies. Additional iterations therefore tend to amplify oscillation and partial rewrites rather than produce stable, cumulative progress. Simply allocating more iterations within the same monolithic execution loop does not reliably improve long-horizon performance, indicating that the limitation lies in execution structure rather than compute budget.

% \subsection{SWE-Primitive Driven Coordination Improves Multi-Agent Performance}
% We compare single-agent baselines with \textsc{Caid} in Table \ref{tab:main-results} and observe a consistent advantage for structured multi-agent execution on both benchmarks. On PaperBench, we observe the clearest signal is that multi-agent coordination can unlock large gains for weaker single-agent runs, for example MiniMax 2.5 reaches 37.1\% under multi-agent while its single-agent score is only 10.4\%. At the same time, the benefit is not restricted to weak baselines: even with Claude 4.5, our multi-agent coordination yields a higher score than the single-agent baseline (63.3\% versus 57.2\%). On Commit0-Lite, the same direction holds in Table \ref{tab:main-results}. Claude 4.5 improves to 59.1\% under multi-agent compared with 53.1\% for single-agent, and MiniMax 2.5 reaches 57.0\% under multi-agent. These comparisons show that the performance gap is not explained by changing the underlying model, but by changing the multi-agent coordination structure. \textsc{Caid} with a central manager to control the overall implementation progress which makes asynchronous and parallel work usable by isolating engineers in independent worktrees and enforcing integration through explicit merge and test validation, so progress consolidates into a coherent, test-passing repository state instead of being destabilized by long-horizon error accumulation.
% We observe that \textsc{Caid} improves final scores across models and benchmarks, indicating that multi-agent collaboration becomes reliably beneficial only when parallel work is \emph{isolated} and \emph{integrated} through explicit coordination mechanisms. On PaperBench, gains vary across models (e.g., MiniMax 2.5 shows the largest improvement), but the consistent pattern is that single-agent execution remains disproportionately fragile in long-horizon shared-artifact settings: modest local errors compound and become difficult to recover from within a single execution thread. A single agent must interleave task delegation, implementation, experiment reproduction, and test verification within one evolving context. As execution unfolds, intermediate decisions are easily overwritten or contradicted, leading to instability across extended chains of reasoning and modification. In contrast, \textsc{Caid} externalizes progress into isolated \texttt{git worktrees}, so that each engineer operates on a stable snapshot of the repository. Integration is not implicit but enforced explicitly through \texttt{git merge} and executable tests, ensuring that inconsistent changes surface early rather than silently accumulating into late-stage failure. Under this structure, parallel progress becomes \emph{usable}: isolation prevents destructive interference among engineers, and structured integration (merge combined with test validation) acts as a gate that either consolidates consistent changes or rejects conflicting ones. The observed improvement therefore does not arise from simply adding more computational resources or agents, but from altering how execution unfolds across steps. Notably, improvements persist even when single-agent baselines are already strong. On PaperBench, Claude 4.5 Sonnet and GLM 4.7 achieve substantial baseline performance under single-agent execution, yet coordinated multi-agent execution further increases final scores. This suggests that the gains come from eliminating interference and enforcing integration, rather than from ``trying harder'' within the same monolithic execution loop. Multi-agent coordination introduces measurable overhead in runtime and cost (Table~2), since more agents participate and communicate through structured protocols. The improvements are therefore not attributable to free parallelism. Instead, they emerge when additional parallel work is disciplined by isolation and integration mechanisms that prevent wasted, conflicting effort. As Commit0 results complete (Table~2), we observe the same qualitative trend: structured coordination improves end-to-end library correctness, consistent with the pattern on PaperBench. Overall, these results support the central claim of this section: without isolation and structured integration, adding more agents does not reliably improve long-horizon outcomes. \textsc{Caid} makes parallel execution effective by preventing interference and transforming integration into an explicit, test-validated step.

% \subsection{Coordinated Multi-Agent with Isolated Delegation}
% We observe that \textsc{Caid} exhibits systematic improvement of the final score across all three LLMs and both PaperBench and Commit0. In PaperBench, the size of the gain differs sharply across models (i.e., MiniMax 2.5 has 26.7\% improvement of the final score), which reflects a structural weakness of single-agent execution in long-horizon SWE tasks. A single agent struggles to reliably switch between task delegation, experiment reproduction, implementation, and test verification without losing intermediate progress, leading to instability across extended execution chains. Under \textsc{Caid}, this instability is substantially reduced, resulting in a qualitative shift from frequent long-horizon breakdowns under single-agent execution to sustained task completion. Even with stronger single-agent baselines, Claude 4.5 achieves a final score of 63.3\% and GLM 4.7 achieves 44.7\% on PaperBench, showing that coordination strengthens already-competent settings rather than merely rescuing weak ones. This suggests that the isolated delegation mechanism alters how execution unfolds across steps, rather than simply benefiting from increased computational resources. Although we parallelize engineer agents and design an asynchronous communication mechanism between the central manager and engineer agents, runtime and cost still increase under multi-agent coordination because more agents participate in the development process and interact through the communication protocol, which is an expected overhead of collaborative execution. Similarly, we observe the same qualitative trend for the remaining Commit0 entries (Table 1, XX), matching the direction observed on PaperBench. These highlight that \textsc{Caid} reduces the frequency of execution breakdowns that occur when a single agent must repeatedly re-contextualize across delegation, implementation, and verification.

% \subsection{Strategy Implications for Long-Horizon SWE Tasks}

% \gncomment{This seems pretty minor for a section, could we just incorporate it into the previous section?}

% The third column in Table \ref{tab:main-results} reveals a clear strategic implication. In long-horizon shared-artifact tasks, multi-agent coordination should not be treated as a fallback after single-agent failure. The Single-Agent + Multi-Agent setting approximates a common practical strategy: first attempt the task with a single agent, and switch to coordinated execution only if the initial attempt does not succeed. Table \ref{tab:main-results} shows that this strategy incurs nearly additive runtime and cost, while the final performance remains close to the direct multi-agent result. For example, on PaperBench with Claude Sonnet 4.5, the single-agent score is 57.2\%, and the multi-agent score is 63.3\%. The combined strategy reaches 66.8\%, only slightly above the multi-agent result, but the runtime increases from 2080.4s to 3883.9s, and the cost rises from 9.3 to 12.6. For GLM 4.7 on PaperBench, the multi-agent score is 45.4\%, and the combined strategy reaches 48.5\%, while runtime increases from 1449.4s to 2626.9s and cost from 4.7 to 7.5.  On Commit0-Lite, the pattern is even clearer. For MiniMax 2.5, the multi-agent score is 57.0\%, and the combined strategy remains 57.0\%, yet runtime increases from 1908.6s to 2660.7s and cost from 4.5 to 6.1. In this case, the additional single-agent phase does not contribute to a performance gain while strictly increasing the computation. These results reveal that the combined strategy does not compound performance, but largely accumulates computation. The final score is effectively determined by the stronger execution, whereas runtime and cost reflect the sum of both phases. In practice, this means paying the full cost of a single-agent attempt without obtaining a proportional performance improvement. For long-horizon tasks characterized by interdependent components and test-gated integration, we therefore advocate directly adopting SWE-primitive-driven multi-agent coordination rather than sequentially escalating from single-agent execution. Early isolation and structured integration are not optional optimizations, but necessary conditions for efficient progress.
% \jy{
% \begin{enumerate}
%     \item Multi-agent = better performance
%     \item For long-horizon task, Caid is all your need.
% \end{enumerate}
% }