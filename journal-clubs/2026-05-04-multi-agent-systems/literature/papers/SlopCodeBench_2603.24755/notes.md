# Notes

This paper matters because it separates "passes tests now" from "remains extensible later" for coding agents.

Use it when arguing for long-horizon coding benchmarks, hidden tests, carried workspaces, or maintainability metrics beyond pass rate. It is also useful when designing agent coding workflows that need architectural discipline across repeated edits.

Key terms: SlopCodeBench, checkpoint trajectory, structural erosion, verbosity, high cyclomatic complexity, carried workspace, black-box testing, anti-slop prompt.
